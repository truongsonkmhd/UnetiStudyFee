server:
  port: ${SERVER_PORT:8097}

spring:
  application:
    name: uneti_study

  datasource:
    url: ${DB_URL:jdbc:postgresql://postgres:5432/uneti_study}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
    enabled: false

  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB
      enabled: true

  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest
    listener:
      simple:
        acknowledge-mode: manual
        retry:
          enabled: false

  jpa:
    hibernate:
        ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false

  data:
    redis:
      host: ${REDIS_HOST:redis}
      port: ${REDIS_PORT:6379}
      timeout: 2000ms

  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=1000,expireAfterWrite=900s,recordStats

# Custom Cache Configuration
app:
  cache:
    type: caffeine
    default-ttl: 900
    default-max-size: 1000
    record-stats: true
    write-behind-enabled: false
    write-behind-delay-ms: 5000
    write-behind-batch-size: 100
    caches:
      user_by_id:
        ttl: 900
        max-size: 1000
        eviction-policy: LRU
      user_by_username:
        ttl: 900
        max-size: 1000
        eviction-policy: LRU
      course_published_tree:
        ttl: 3600
        max-size: 500
        eviction-policy: LRU
      quiz_by_id:
        ttl: 900
        max-size: 500
        eviction-policy: LRU
      roles:
        ttl: 21600
        max-size: 100
        eviction-policy: LFU
      permissions:
        ttl: 21600
        max-size: 100
        eviction-policy: LFU

pocketbase:
  url: ${POCKETBASE_URL:http://localhost:8090}
  external-url: ${POCKETBASE_EXTERNAL_URL:http://localhost:8090}
  admin-email: ${POCKETBASE_ADMIN_EMAIL:admin@example.com}
  admin-password: ${POCKETBASE_ADMIN_PASSWORD:password}

security:
  jwt:
    signerKey: "1TjXchw5FloESb63Kc+DFhTARvpWL4jUGCwfGWxuG5SIf/1y/LgJxHnMqaF6A/ij"
    token-validity-in-seconds: 900000
    refresh-token-validity-in-seconds: 259200000
    refresh-token-validity-in-seconds-for-remember-me: 2592000000


jhipster:
  security:
    authentication:
      jwt:
        base64-secret: ODFjYzVlMDQ2NDlhNjM2NDRiN2ZjYWRhNzlhY2ExODk2NzNiMWQzZmI1NDBiYzA2N2FhZWU4OGZjYTQyZjU1YjllY2YzNDQzNDg3MDkxYjhkMzc2YTcyNzhiMWY1OWRkYzUwN2FhNGQ4MjUxYzVjNGEzODU3MTA2MmU4MjcwODg=
        token-validity-in-seconds: 86400
        token-validity-in-seconds-for-remember-me: 2592000
  cors:
    allowed-origins:
      - "http://localhost:3000"
      - "http://localhost:5173"
    allowed-methods: "*"
    allowed-headers: "*"
    exposed-headers: "Authorization,Link,X-Total-Count,X-${jhipster.clientApp.name}-alert,X-${jhipster.clientApp.name}-error,X-${jhipster.clientApp.name}-params"
    allow-credentials: true
    max-age: 1800

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      probes:
        enabled: true
  health:
    livenessState:
      enabled: true
    readinessState:
      enabled: true
  prometheus:
    metrics:
      export:
        step: 5s
        enabled: true

logging:
  level:
    root: INFO
    web: OFF
    org.springframework: INFO
    org.apache: OFF
    kafka: OFF
